#Build the wrapper library using the following command:
```
cd ../
make lib
cd python
sudo python setup.py install
```
then you can try to run the little example
```
python example.py
```
This is mostly a command line computation and there is no graphical tool. 
You may want to use the R module or you can help to create similar graphical interpretations


